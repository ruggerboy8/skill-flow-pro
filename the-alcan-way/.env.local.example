# Copy this file to ".env.local" (same folder) and paste your real key.
#   cp .env.local.example .env.local
# .env.local is gitignored, so the key never gets committed.

OPENAI_API_KEY=sk-your-key-here
